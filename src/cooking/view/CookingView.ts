import { CustomerDisplayData } from "../types/CustomerDisplayData";

export class CookingView {
    constructor() {
    }

    initialize(customerData: CustomerDisplayData[], label: string, score: number) {
        // Initialize the view
    }
}